# V11.11 Launch Readiness — Ark Pass — 2026-05-06

## Status
V11.11 is deployed to production on Cloudflare Pages.

No DNS edit, live Stripe checkout, live contact submission, or GitHub push was performed in this pass.

Production deployment verified:
- Cloudflare Pages deployment: `https://7eb7d95a.aissisted-website.pages.dev`
- Production domain: `https://aissistedconsulting.com/`
- Local commit: `216f5f8 launch V11.11 website package`

## Source Root
`/Users/standley/Documents/New project 2`

## What Ark changed

### Added production-safe Cloudflare config
- Added `wrangler.toml` for existing Cloudflare Pages project `aissisted-website`.
- Bound D1 database `aissisted-booking` / `702b49f1-4e87-4390-8b36-3da67bc126ff` as `BOOKING_DB`.
- Set non-secret production vars:
  - `PUBLIC_SITE_ORIGIN=https://aissistedconsulting.com`
  - `$225` booking deposit via `BOOKING_RESERVATION_AMOUNT_CENTS=22500`
  - `STRIPE_BOOKING_PRICE_ID=price_1TNNnxP3Zy09i3cc3B8yErot`
  - `BOOKING_REQUIRE_GOOGLE_CALENDAR=true` so booking availability fails closed if Calendar breaks.

### Added existing-production D1 upgrade migration
- Added `migrations/0002_v1111_transactional_hardening.sql`.
- Purpose: upgrade current production D1 from the older booking schema to the V11.11 transactional schema without dropping existing records.
- Adds:
  - `bookings.checkout_idempotency_record_id`
  - `bookings.checkout_audit_id`
  - `agent_idempotency_records`
  - `contact_inquiries`
  - `agent_transaction_audits`
  - required indexes.

### Hardened launch redirects
- Updated `_redirects` with:
  - apex canonical domain redirects for `www.aissistedconsulting.com`, `ocalaaiconsulting.com`, and `www.ocalaaiconsulting.com`.
  - slash normalization for new V11.11 routes.
  - legacy production route redirects from old pages like `/pricing`, `/compare`, `/openclaw`, `/pay`, `/reserve`, etc.
  - docs/tests/db/migrations/package blocking retained.

### Updated local project docs
- Updated `docs/project/README.md`.
- Updated `docs/project/V11.11-website-project-index.md`.

## Current Cloudflare facts checked

### Pages project
`npx wrangler pages project list` shows:
- Project: `aissisted-website`
- Domains include:
  - `aissisted-website.pages.dev`
  - `aissistedconsulting.com`
  - `ocalaaiconsulting.com`
  - `www.ocalaaiconsulting.com`

### Existing production secrets already present
`npx wrangler pages secret list --project-name=aissisted-website` shows encrypted values exist for:
- `GOOGLE_CALENDAR_ID`
- `GOOGLE_OAUTH_CLIENT_ID`
- `GOOGLE_OAUTH_CLIENT_SECRET`
- `GOOGLE_OAUTH_REFRESH_TOKEN`
- `STRIPE_SECRET_KEY`
- `STRIPE_WEBHOOK_SECRET`

No secret values were read.

### Existing D1 state
Remote read only: `aissisted-booking` currently has older base booking tables and existing data:
- `prospects`: 5
- `bookings`: 4
- `booking_events`: 17
- `deposit_credits`: 0

Existing remote DB does **not** yet have the V11.11 idempotency/contact/audit tables, so `0002_v1111_transactional_hardening.sql` must run before live V11.11 booking/contact writes.

## Verification evidence from this pass

### Local checks passed
Command:
```bash
npm run check:site && npm run check:booking-functions && npm run test:booking
```
Result:
- `check:site` passed.
- `check:booking-functions` passed.
- `test:booking` passed: 21/21 tests.

### Migration test passed locally
Simulated the current production base schema in SQLite, then applied:
```bash
migrations/0002_v1111_transactional_hardening.sql
```
Result:
- new tables created.
- `bookings.checkout_idempotency_record_id` exists.
- `bookings.checkout_audit_id` exists.

### Local route/support-file check passed
Local server at `http://127.0.0.1:4192` returned 200 for 30 routes/support files including:
- `/`
- `/services/`
- `/contact/`
- `/book/`
- `/agent.json`
- `/.well-known/agent.json`
- `/.well-known/agent-skills/index.json`
- `/llms.txt`
- JSON API support files
- knowledge markdown files

JSON/XML parse checks passed in the route script.

### Secret scan result
No live secret/key strings found in deploy source. Only false positives appeared in tests/docs/helper code containing placeholder private-key text patterns.

## Production launch actions completed

1. Ran production D1 upgrade migration `0002_v1111_transactional_hardening.sql`.
2. Verified new D1 tables/columns exist remotely:
   - `agent_idempotency_records`
   - `contact_inquiries`
   - `agent_transaction_audits`
   - `bookings.checkout_idempotency_record_id`
   - `bookings.checkout_audit_id`
3. Deployed clean V11.11 bundle to Cloudflare Pages project `aissisted-website`.
4. Added/verified middleware protections for canonical host redirects and blocked local project paths.
5. Ran live smoke checks.

## Live smoke evidence

Passed:
- 30 production routes/support files returned 200 and JSON/XML parsed where applicable.
- `/api/book/availability` returned 200 JSON using `availabilitySource: "google-calendar"`.
- `www.aissistedconsulting.com` redirects to `https://aissistedconsulting.com/`.
- `ocalaaiconsulting.com` redirects to `https://aissistedconsulting.com/`.
- `www.ocalaaiconsulting.com` redirects to `https://aissistedconsulting.com/`.
- legacy routes `/pricing`, `/openclaw`, and `/pay` redirect to the intended V11.11 routes.
- blocked local/project paths now return true 404 with `X-Robots-Tag: noindex, nofollow`.

## Remaining post-launch items

- Confirm Stripe webhook endpoint in Stripe dashboard is still pointed at `https://aissistedconsulting.com/api/book/webhook`.
- Run a controlled Stripe Checkout test-mode or live smoke only if PJ explicitly wants that transaction created.
- Run deployed SlipperyAPeI verification if/when the CLI is available.
- Push the local commit to a remote only if PJ wants this package preserved in GitHub.

name = "aissisted-website"
compatibility_date = "2026-03-10"
pages_build_output_dir = "."

# NOTE ON SECRETS: Do NOT add STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET,
# or any other credential here. `[vars]` is committed to git and also
# overwrites the Cloudflare Pages dashboard env vars on every
# `wrangler pages deploy`. Use `npx wrangler pages secret put NAME
# --project-name=aissisted-website` for secrets. Secrets are stored
# separately from [vars] and survive deploys.
#
# Current Pages secrets (do not duplicate here):
#   - STRIPE_SECRET_KEY  (set 2026-04-18, source ~/.openclaw/secrets/stripe.env)
#
# Non-secret config that IS safe to live here:
[vars]
PUBLIC_SITE_ORIGIN = "https://aissistedconsulting.com"
BOOKING_BUSINESS_TITLE = "AIssisted Consulting"
BOOKING_SUPPORT_EMAIL = "pj@aissistedconsulting.com"
BOOKING_CURRENCY = "usd"
BOOKING_TIMEZONE = "America/New_York"
BOOKING_HOLD_MINUTES = "30"
BOOKING_LOOKAHEAD_DAYS = "21"
BOOKING_POLICY_VERSION = "2026-04-06"
BOOKING_RESERVATION_AMOUNT_CENTS = "22500"
STRIPE_BOOKING_PRICE_ID = "price_1TNNnxP3Zy09i3cc3B8yErot"

[[d1_databases]]
binding = "BOOKING_DB"
database_name = "aissisted-booking"
database_id = "702b49f1-4e87-4390-8b36-3da67bc126ff"
preview_database_id = "702b49f1-4e87-4390-8b36-3da67bc126ff"
